syntax = "proto3";

package coverit.project.v1;

import "coverit/common/v1/types.proto";
import "validate/validate.proto";

///////////////
// Request DTOs
///////////////

message CreateProjectRequest {
  string name = 1 [
    (validate.rules).string.min_len = 1,
    (validate.rules).string.max_len = 100
  ];

  optional string description = 2 [
    (validate.rules).string.max_len = 500
  ];
}

message UpdateProjectRequest {
  optional string name = 1 [
    (validate.rules).string.min_len = 1,
    (validate.rules).string.max_len = 100
  ];

  optional string description = 2 [
    (validate.rules).string.max_len = 500
  ];
}

message MemberRequest {
  string email = 1 [
    (validate.rules).string.email = true,
    (validate.rules).string.min_len = 1,
    (validate.rules).string.max_len = 100
  ];

  string role = 2;
}

message AddMembersRequest {
  repeated MemberRequest members = 1 [
    (validate.rules).repeated.min_items = 1,
    (validate.rules).repeated.max_items = 100
  ];
}

message UpdateMemberRequest {
  string id = 1;
  string role = 2;
}

message RemoveMembersRequest {
  repeated string emails = 1 [
    (validate.rules).repeated.min_items = 1,
    (validate.rules).repeated.max_items = 100,
    (validate.rules).repeated.items.string.email = true
  ];
}

////////////////
// Response DTOs
////////////////

message CreateProjectResponse {
  string id = 1;
}

message ProjectResponse {
  string id = 1;
  string name = 2;
  string description = 3;
  repeated Member members = 4;
}

message Member {
  coverit.common.v1.UserInfo user = 1;
  string role = 2;
}
